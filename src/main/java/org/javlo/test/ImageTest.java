package org.javlo.test;

import java.io.IOException;

public class ImageTest {

	public static void main(String[] args) throws IOException {
		/*MBFImage image = ImageUtilities.readMBF(new File("c:/trans/1686940185.jpg"));		
		FaceDetector<CLMDetectedFace,FImage> faceDetector = new CLMFaceDetector();
		List<CLMDetectedFace> detectedFace = faceDetector.detectFaces(image.flatten());
		for (CLMDetectedFace faces : detectedFace) {			
		   image.drawPolygon(faces.getBounds().asPolygon(), RGBColour.RED);		   
		}
		DisplayUtilities.display(image);*/
	}
}
